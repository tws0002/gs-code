frame_padding: 5
frame_delimiter: '.'
version_prefix: 'v'
version_incr: 1
version_padding: 3
stages:
  - pitch
  - previs
  - production
departments:
- admin
- design
- producer
- 3d_production
- 2d_production
- compositing
- edit
tasks:
- asset
- animation
- lighting
- composite
- edit
status:
- 'ready'
- 'not started'
- 'in progress'
- 'finished'
- 'cbb'
- 'approved'
- 'on hold'
- 'dead'
data_structs:
  '3d_asset_lib':
    root_path: "<file_share>/<job>/04_production/01_cg/01_MAYA/scenes/02_cg_elements"
    public_root: "<root_path>"
    private_root: "<root_path>/_versions"
  'asset_group':
    root_path: "<file_share>/<job>/<stage>/<asset_lib>"
    public_root: "<root_path>"
    private_root: "<root_path>"      
  'asset':
    root_path: "<fileshare>/<job>/<stage>/<asset_lib>/<asset_group>"
    public_root: "<root_path>/publish"
    private_root: "<root_path>/user/<username>"
  '3d_shot_lib':
    root_path: "<file_share>/<job>/04_production/01_cg/01_MAYA/scenes/02_cg_elements"
    public_root: "<root_path>"
    private_root: "<root_path>/_versions"
  'shot_group':
    root_path: "<fileshare>/<job>/<stage>/sequences"
    public_root: "<root_path>"
    private_root: "<root_path>"
  'shot':
    root_path: "<fileshare>/<job>/<stage>/sequences/<sequence>"
    public_root: "<root_path>/<shot>/publish"
    private_root: "<root_path>/<shot>/user/<username>"
task_struct:
  'default':
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"
  'edit':
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"
  'design':
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"
  'design':
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"
  'anim':
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"
  'comp':
    inherits: 'default'
    root_path: "<task_root>/"
    public_root: "<task_root>/public/<package>"
    private_root: "<task_root>/user/<package>"
    out_cache_path: "<task_root>/public/cache/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_render_path: "<fileshare>/<job>/<stage>/renders/<sequence>/<shot>/<task>/<version>"
    out_cache_file: "<sequence>_<shot>_<task>_<version>.<ext>"
    out_scenefile: "<sequence>_<shot>_<task>_<version>.<ext>"